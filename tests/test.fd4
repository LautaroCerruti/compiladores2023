let f (x:Nat) : Nat =
    let u1 : Nat = x in
    let x2 : Nat = x+1 in
    let u2 : Nat = x2 in
    let x3 : Nat = x2+1 in
    let u3 : Nat = x3 in
    let x4 : Nat = x3+1 in
    let u4 : Nat = x4 in
    print "AAAAAAAAAA" x

let a : Nat = f 1